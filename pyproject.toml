[project]
name = "mul-retrieval"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "requests",
    "beautifulsoup4",
    "pandas",
    "tqdm",
    "jupyter",
    "nbconvert",
    "ipykernel",
    "streamlit>=1.55.0",
]
